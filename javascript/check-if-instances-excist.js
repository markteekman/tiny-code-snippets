// select multiple instances
const instances = document.querySelectorAll('.instance')

// check if instances excist before executing code
if (instances.length > 0) {
  instances.forEach(instance => {
    // ...
  })
}
